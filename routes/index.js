const express = require("express");
const writerRoutes = require("./writerRoutes");
const app = express.Router();

function route(app) {
    // app.use("/online-news",);
    // app.use("/online-news/main",);
    // app.use("/online-news/writer",);
    // app.use("/online-news/editor",);
    // app.use("/online-news/admin",);
}